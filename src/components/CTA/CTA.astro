<section class="cta" data-animate>
  <div class="cta__bg-text" aria-hidden="true">PRUEBA</div>

  <div class="cta__container">
    <header class="cta__content">
      <p class="cta__tag" data-animate>// CTA FINAL</p>
      <h2 class="cta__title" data-animate>
        <span class="cta__title-line">¿LISTO PARA</span>
        <span class="cta__title-line cta__title-line--accent">EMPEZAR?</span>
      </h2>
      <p class="cta__desc" data-animate>
        Tu primera clase es gratis. Vení a conocer el gimnasio, probá una clase y sentí la diferencia. Sin compromiso, solo entrenamiento de verdad.
      </p>
      <div class="cta__buttons" data-animate>
        <a href="#" class="cta__btn cta__btn--primary">
          Reservá tu clase de prueba gratis
          <span class="cta__btn-arrow" aria-hidden="true">→</span>
        </a>
        <a href="https://wa.me/541123456789" target="_blank" rel="noopener noreferrer" class="cta__btn cta__btn--whatsapp">
          <span class="cta__btn-icon" aria-hidden="true">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" fill="currentColor"/>
            </svg>
          </span>
          Consultanos por WhatsApp
        </a>
      </div>
    </header>
  </div>
</section>

<style>
  .cta {
    position: relative;
    padding: var(--space-xl) var(--container-padding);
    background: var(--color-bg);
    overflow: hidden;
    border-top: 1px solid var(--color-border);
  }

  .cta__bg-text {
    position: absolute;
    top: 50%;
    right: -0.1em;
    transform: translateY(-50%);
    font-family: var(--font-display);
    font-size: clamp(5rem, 22vw, 18rem);
    font-weight: var(--fw-extrablack);
    color: transparent;
    -webkit-text-stroke: 1px var(--color-border);
    opacity: 0.08;
    white-space: nowrap;
    pointer-events: none;
    user-select: none;
    line-height: 1;
    z-index: 0;
  }

  .cta__container {
    position: relative;
    z-index: 1;
    max-width: var(--container-max);
    margin: 0 auto;
    display: flex;
    justify-content: center;
  }

  .cta__content {
    text-align: center;
    max-width: 640px;
  }

  .cta__tag {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    font-weight: var(--fw-semibold);
    letter-spacing: var(--tracking-widest);
    color: var(--color-accent);
    text-transform: uppercase;
    margin-bottom: var(--space-sm);
  }

  .cta__title {
    font-family: var(--font-display);
    font-weight: var(--fw-extrablack);
    text-transform: uppercase;
    line-height: 0.92;
    margin-bottom: var(--space-lg);
  }

  .cta__title-line {
    display: block;
    font-size: clamp(2.4rem, 8vw, 4.5rem);
    color: var(--color-text);
  }

  .cta__title-line--accent {
    color: var(--color-accent);
  }

  .cta__desc {
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: 1.7;
    color: var(--color-text-muted);
    margin-bottom: var(--space-xl);
  }

  .cta__buttons {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    align-items: center;
  }

  .cta__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    font-family: var(--font-display);
    font-size: var(--text-base);
    font-weight: var(--fw-bold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    cursor: pointer;
    transition: all var(--transition-fast);
    border: none;
    padding: 1.1rem 2.5rem;
  }

  .cta__btn--primary {
    background: var(--color-accent);
    color: var(--color-text);
  }

  .cta__btn--primary:hover {
    background: var(--color-accent-hover);
  }

  .cta__btn-arrow {
    display: inline-block;
    transition: transform var(--transition-fast);
  }

  .cta__btn--primary:hover .cta__btn-arrow {
    transform: translateX(4px);
  }

  .cta__btn--whatsapp {
    background: transparent;
    color: var(--color-text-muted);
    border: 1px solid var(--color-border);
  }

  .cta__btn--whatsapp:hover {
    color: var(--color-text);
    border-color: var(--color-text);
  }

  .cta__btn-icon {
    display: inline-flex;
    align-items: center;
  }

  .cta__tag {
    transition-delay: 0s;
  }

  .cta__title {
    transition-delay: 0.1s;
  }

  .cta__desc {
    transition-delay: 0.15s;
  }

  .cta__buttons {
    transition-delay: 0.25s;
  }

  @media (min-width: 768px) {
    .cta {
      padding: var(--space-2xl) var(--container-padding);
    }

    .cta__buttons {
      flex-direction: row;
    }
  }
</style>
